<% if flash.count > 0 %>


  <% flash.each do |type, message| %>
    <div class="alert <%= bootstrap_alert_class_for(type) %>" role="alert" style="margin-top: 10px">
      <button class="close" data-dismiss="alert">×</button>
      <%= message %>
    </div>
  <% end %>

<% end %>